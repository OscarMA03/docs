---
title: 'Create Lead (Simplified)'
openapi: 'POST /create-lead'
---

## Create Simple Lead

Create a simplified lead with core fields only. The `secretKey` is automatically set from the `BUILDER_PRIME_API_KEY` environment variable.

### Headers

<ParamField path="header" type="string" name="x-builder-prime-api-key" required>
  Builder Prime API key for authentication
</ParamField>

<ParamField path="header" type="string" name="x-api-key" required>
  API key for authentication
</ParamField>

<ParamField path="header" type="string" name="x-company-url-append" required>
  Company URL append value (e.g., "stage", "prod")
</ParamField>

<ParamField path="header" type="string" name="Content-Type" required>
  application/json
</ParamField>

### Request Body

<ParamField path="body" type="string" name="firstName" required>
  Lead's first name
</ParamField>

<ParamField path="body" type="string" name="lastName">
  Lead's last name (optional, defaults to empty string)
</ParamField>

<ParamField path="body" type="string" name="email" required>
  Lead's email address
</ParamField>

<ParamField path="body" type="string" name="phone" required>
  Lead's phone number
</ParamField>

<ParamField path="body" type="string" name="addressLine1" required>
  Lead's address (whatever address the user provides)
</ParamField>

<ParamField path="body" type="string" name="city">
  Lead's city (optional)
</ParamField>

<ParamField path="body" type="string" name="state">
  Lead's state (optional)
</ParamField>

<ParamField path="body" type="string" name="zip">
  Lead's ZIP code (optional)
</ParamField>

### Example Request

```bash
POST /create-lead
Content-Type: application/json
x-builder-prime-api-key: <your-builder-prime-api-key>
x-api-key: <your-api-key>
x-company-url-append: <your-company-url-append>

{
  "firstName": "John",
  "lastName": "Doe",
  "email": "john.doe@example.com",
  "phone": "+1234567890",
  "addressLine1": "123 Main St",
  "city": "Anytown",
  "state": "NY",
  "zip": "12345"
}
```

### Example Response

```json
{
  "success": true,
  "data": {
    "opportunityId": "12345",
    "firstName": "John",
    "lastName": "Doe",
    "email": "john.doe@example.com",
    "phone": "+1234567890",
    "addressLine1": "123 Main St",
    "city": "Anytown",
    "state": "NY",
    "zip": "12345",
    "leadStatus": "Lead Received",
    "leadSource": "API"
  },
  "message": "Successfully created lead for John Doe",
  "leadInfo": {
    "name": "John Doe",
    "firstName": "John",
    "lastName": "Doe",
    "email": "john.doe@example.com",
    "phone": "+1234567890",
    "addressLine1": "123 Main St",
    "city": "Anytown",
    "state": "NY",
    "zip": "12345",
    "fullAddress": "123 Main St, Anytown, NY, 12345"
  },
  "note": "secretKey automatically set from BUILDER_PRIME_API_KEY"
}
```

### Error Response

```json
{
  "success": false,
  "error": "Failed to create lead",
  "details": "firstName is required"
}
```

### Use Cases

- **Quick Lead Creation**: Create leads with minimal required information
- **Form Integration**: Easy integration with web forms
- **API Simplification**: Simplified lead creation without complex payloads
