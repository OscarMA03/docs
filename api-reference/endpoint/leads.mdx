---
title: 'Leads'
openapi: 'GET /leads'
---

## Get Leads

Retrieve leads/opportunities with optional filtering.

<ParamField path="query" type="string" name="opportunity-id">
  Filter by specific opportunity ID
</ParamField>

<ParamField path="query" type="string" name="last-modified-since">
  Filter leads modified since timestamp (Unix timestamp)
</ParamField>

<ParamField path="query" type="string" name="lead-status">
  Filter by lead status
</ParamField>

<ParamField path="query" type="string" name="lead-source">
  Filter by lead source
</ParamField>

<ParamField path="query" type="string" name="dialer-status">
  Filter by dialer status
</ParamField>

<ParamField path="query" type="string" name="phone">
  Filter by phone number
</ParamField>

<ParamField path="query" type="number" name="limit">
  Maximum number of leads to return
</ParamField>

<ParamField path="query" type="number" name="page">
  Page number for pagination
</ParamField>

### Example Request

```bash
GET /leads?lead-status=Lead Received&limit=10&page=0
```

### Example Response

```json
[
  {
    "opportunityId": "12345",
    "firstName": "John",
    "lastName": "Doe",
    "email": "john.doe@example.com",
    "phone": "+1234567890",
    "addressLine1": "123 Main St",
    "city": "Anytown",
    "state": "NY",
    "zip": "12345",
    "leadStatus": "Lead Received",
    "leadSource": "API",
    "lastModifiedDate": 1705312200000
  }
]
```

---

## Create Lead

<ParamField path="body" type="object" name="leadData" required>
  Lead data for creation
</ParamField>

### Request Body

<ParamField path="body" type="string" name="firstName" required>
  Lead's first name
</ParamField>

<ParamField path="body" type="string" name="lastName" required>
  Lead's last name
</ParamField>

<ParamField path="body" type="string" name="email" required>
  Lead's email address
</ParamField>

<ParamField path="body" type="string" name="mobilePhone" required>
  Lead's mobile phone number
</ParamField>

<ParamField path="body" type="string" name="addressLine1" required>
  Lead's address line 1
</ParamField>

<ParamField path="body" type="string" name="city">
  Lead's city
</ParamField>

<ParamField path="body" type="string" name="state">
  Lead's state
</ParamField>

<ParamField path="body" type="string" name="zip">
  Lead's ZIP code
</ParamField>

<ParamField path="body" type="string" name="leadStatusName">
  Lead status (default: "Lead Received")
</ParamField>

<ParamField path="body" type="string" name="leadSourceName">
  Lead source (default: "API")
</ParamField>

<ParamField path="body" type="string" name="secretKey" required>
  API secret key (automatically set from environment)
</ParamField>

### Example Request

```bash
POST /leads
Content-Type: application/json

{
  "firstName": "John",
  "lastName": "Doe",
  "email": "john.doe@example.com",
  "mobilePhone": "+1234567890",
  "addressLine1": "123 Main St",
  "city": "Anytown",
  "state": "NY",
  "zip": "12345",
  "leadStatusName": "Lead Received",
  "leadSourceName": "API",
  "secretKey": "your-api-key"
}
```

### Example Response

```json
{
  "success": true,
  "data": {
    "opportunityId": "12345",
    "firstName": "John",
    "lastName": "Doe",
    "email": "john.doe@example.com",
    "phone": "+1234567890",
    "addressLine1": "123 Main St",
    "city": "Anytown",
    "state": "NY",
    "zip": "12345",
    "leadStatus": "Lead Received",
    "leadSource": "API"
  },
  "message": "Successfully created lead for John Doe"
}
```
