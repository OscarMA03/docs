---
title: 'Update Agent Configuration'
openapi: 'PATCH /update-config/{llm_id}'
---

## Update Agent Configuration

Updates agent configuration settings for an existing agent by LLM ID.

### Headers

<ParamField path="header" type="string" name="x-api-key" required>
  API key for authentication
</ParamField>

<ParamField path="header" type="string" name="Content-Type" required>
  application/json
</ParamField>

### Path Parameters

<ParamField path="path" type="string" name="llm_id" required>
  Retell LLM identifier for the agent to update
</ParamField>

### Request Body

<ParamField path="body" type="object" name="configuration" required>
  Company configuration object to update
</ParamField>

<ParamField path="body.configuration" type="string" name="company_url">
  Company's API endpoint
</ParamField>

<ParamField path="body.configuration" type="string" name="company_name">
  Official business name
</ParamField>

<ParamField path="body.configuration" type="string" name="company_number">
  Callback number for follow-ups
</ParamField>

<ParamField path="body.configuration" type="string" name="service_label_type">
  What to call the appointment (e.g., "In Home Consultation")
</ParamField>

<ParamField path="body.configuration" type="string" name="ai_disclosure">
  Whether agent announces it's AI (e.g., "true")
</ParamField>

<ParamField path="body.configuration" type="string" name="business_hours">
  When to offer appointments
</ParamField>

<ParamField path="body.configuration" type="string" name="service_areas">
  Geographic restrictions
</ParamField>

<ParamField path="body.configuration" type="string" name="callback">
  Create task to callback at later time
</ParamField>

<ParamField path="body.configuration" type="string" name="custom_greeting">
  Custom greeting text
</ParamField>

<ParamField path="body.configuration" type="string" name="agent_name">
  Agent name (e.g., "Sally")
</ParamField>

<ParamField path="body.configuration" type="string" name="services_supported">
  Comma-separated list of services included (e.g., "windows, doors, replacements")
</ParamField>

<ParamField path="body.configuration" type="string" name="services_not_included">
  Comma-separated list of services excluded (e.g., "repairs, roofs")
</ParamField>

<ParamField path="body.configuration" type="string" name="price_discussion">
  Instructions for discussing pricing with customers
</ParamField>

<ParamField path="body.configuration" type="string" name="pricing_dialogue">
  Script or dialogue for pricing conversations
</ParamField>

<ParamField path="body.configuration" type="string" name="promotion_mentions">
  Information about promotions or special offers to mention
</ParamField>

<ParamField path="body.configuration" type="string" name="qualifications">
  Qualifications or credentials to mention (e.g., licenses, certifications)
</ParamField>

### Example Request

```bash
PATCH /update-config/llm_abc123xyz
Content-Type: application/json
x-api-key: <your-api-key>

{
  "configuration": {
    "company_url": "<string>",
    "company_name": "<string>",
    "company_number": "<string>",
    "service_label_type": "<string>",
    "ai_disclosure": "<string>",
    "business_hours": "<string>",
    "service_areas": "<string>",
    "callback": "<string>",
    "custom_greeting": "<string>",
    "agent_name": "<string>",
    "services_supported": "<string>",
    "services_not_included": "<string>",
    "price_discussion": "<string>",
    "pricing_dialogue": "<string>",
    "promotion_mentions": "<string>",
    "qualifications": "<string>"
  }
}
```

### Example Response

```json
{
  "success": true,
  "data": {
    "configuration": {
      "company_url": "https://company-api.example.com",
      "company_name": "Example Company",
      "company_number": "+1234567890",
      "service_label_type": "In Home Consultation",
      "ai_disclosure": "true",
      "business_hours": "Monday 9am-5pm, Friday 9am-5pm",
      "service_areas": "New York, New Jersey",
      "callback": "callback_task",
      "custom_greeting": "Hi, thanks for calling Example Company",
      "agent_name": "Sally",
      "services_supported": "windows, doors, replacements",
      "services_not_included": "repairs, roofs",
      "price_discussion": "Discuss pricing after understanding customer needs",
      "pricing_dialogue": "Our pricing is competitive and varies based on project scope",
      "promotion_mentions": "Ask about our current seasonal promotions",
      "qualifications": "Licensed and insured, 20+ years of experience"
    }
  },
  "error": null
}
```

### Error Response

```json
{
  "success": false,
  "data": null,
  "error": "LLM ID not found or update failed"
}
```

### Response Fields

<ParamField path="response" type="boolean" name="success">
  Indicates if the request was successful
</ParamField>

<ParamField path="response" type="object" name="data">
  Response data object
</ParamField>

<ParamField path="response" type="object" name="data.configuration">
  Object containing all updated configuration variables for the agent
</ParamField>

<ParamField path="response" type="string" name="data.configuration.company_url">
  Company's API endpoint
</ParamField>

<ParamField path="response" type="string" name="data.configuration.company_name">
  Official business name
</ParamField>

<ParamField path="response" type="string" name="data.configuration.company_number">
  Callback number for follow-ups
</ParamField>

<ParamField path="response" type="string" name="data.configuration.service_label_type">
  What to call the appointment
</ParamField>

<ParamField path="response" type="string" name="data.configuration.ai_disclosure">
  Whether agent announces it's AI
</ParamField>

<ParamField path="response" type="string" name="data.configuration.business_hours">
  When to offer appointments
</ParamField>

<ParamField path="response" type="string" name="data.configuration.service_areas">
  Geographic restrictions
</ParamField>

<ParamField path="response" type="string" name="data.configuration.callback">
  Create task to callback at later time
</ParamField>

<ParamField path="response" type="string" name="data.configuration.custom_greeting">
  Custom greeting text
</ParamField>

<ParamField path="response" type="string" name="data.configuration.agent_name">
  Agent name
</ParamField>

<ParamField path="response" type="string" name="data.configuration.services_supported">
  Comma-separated list of services included
</ParamField>

<ParamField path="response" type="string" name="data.configuration.services_not_included">
  Comma-separated list of services excluded
</ParamField>

<ParamField path="response" type="string" name="data.configuration.price_discussion">
  Instructions for discussing pricing with customers
</ParamField>

<ParamField path="response" type="string" name="data.configuration.pricing_dialogue">
  Script or dialogue for pricing conversations
</ParamField>

<ParamField path="response" type="string" name="data.configuration.promotion_mentions">
  Information about promotions or special offers to mention
</ParamField>

<ParamField path="response" type="string" name="data.configuration.qualifications">
  Qualifications or credentials to mention
</ParamField>

### Use Cases

- **Configuration Updates**: Update existing agent configuration settings
- **Business Rule Changes**: Modify appointment types, business hours, or service areas
- **Agent Customization**: Update agent name, custom greeting, or disclosure settings
- **Service Management**: Modify supported or excluded services

### Implementation Notes

- Updates configuration for an existing agent identified by the LLM ID in the path
- Only fields included in the request body will be updated (partial updates are supported)
- Fields not included in the request will remain unchanged
- Returns the updated configuration object in the response

